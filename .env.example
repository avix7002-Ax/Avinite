# Supabase Configuration
# These are automatically provisioned by Bolt — do not change unless you know what you're doing.

VITE_SUPABASE_URL=your_supabase_project_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# Production website URL for auth redirect links (password reset emails, OAuth).
# Must match the deployed site URL so reset links open the production site.
VITE_PRODUCTION_URL=https://your-deployed-site.bolt.host

# Production website URL for auth redirect links (password reset emails, OAuth).
# Must match the deployed site URL so reset links open the production site.
VITE_PRODUCTION_URL=https://your-deployed-site.bolt.host
